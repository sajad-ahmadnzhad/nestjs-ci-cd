services:
  backend: 
    container_name: backend
    build:
      dockerfile: Dockerfile
    ports:
      - ${PORT}:${PORT-3000}
    volumes:
      - ./src:/app/src
    networks:
      - app

networks:
  app: 
    driver: bridge